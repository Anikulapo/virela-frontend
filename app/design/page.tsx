export default function DesignPage() {
  return (
    <main className="mx-auto flex w-full max-w-5xl flex-1 flex-col px-6 py-10">
      <p className="text-xs font-medium uppercase tracking-widest text-zinc-400">
        Page 3 · Design
      </p>
      <h1 className="mt-2 text-3xl font-semibold tracking-tight">Design</h1>
      <p className="mt-1 max-w-md text-zinc-500">
        Simple choices, not a blank textbox: color swatches, style tags,
        optional occasion, small notes box with a character limit.
      </p>
    </main>
  );
}