export default function LoginPage() {
  return (
    <main className="mx-auto flex w-full max-w-5xl flex-1 flex-col px-6 py-10">
      <p className="text-xs font-medium uppercase tracking-widest text-zinc-400">
        Page 2 · Log in / sign up
      </p>
      <h1 className="mt-2 text-3xl font-semibold tracking-tight">Log in</h1>
      <p className="mt-1 max-w-md text-zinc-500">
        Short sign-up/log-in form — name, email, phone, password or one-tap
        email link. Leads straight into designing.
      </p>
    </main>
  );
}